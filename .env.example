# Copy this file to .env and fill in real values.
# .env is git-ignored and only loaded in development/test (see Gemfile).
# In production, set these as real environment variables through your
# hosting platform instead - a .env file is not read there.

ADMIN_EMAIL=admin@piladerflice.cz
ADMIN_PASSWORD=change-me-to-something-long-and-random
