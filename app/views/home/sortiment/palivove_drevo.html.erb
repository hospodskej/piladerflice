<%
  products = [
    {
      title: t("sortiment.palivove_drevo.title"),
      descriptions: [
        t("sortiment.palivove_drevo.desc_1"),
        t("sortiment.palivove_drevo.desc_2")
      ],
      images: [
        "sortiment/palivove-drevo1.png",
        "sortiment/palivove-drevo2.png"
      ],
      eshop_link: "/eshop?category=palivove&"
    }
  ]
%>

<main class="main">
  <% products.each_with_index do |product, index| %>
    <%= render partial: "shared/product_row", locals: {
      title: product[:title],
      descriptions: product[:descriptions],
      images: product[:images],
      eshop_link: product[:eshop_link],
      reverse: index.odd?
    } %>
  <% end %>

  <%= render "shared/wood_types" %>

  <section class="how-to-order-detail-wrapper">
    <%= render "shared/jak_objednat" %>
  </section>
</main>
