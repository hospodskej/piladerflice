<%= form_with url: cart_items_path, method: :post, class: "add-to-cart-form" do %>
  <%= hidden_field_tag :catalog_variant_id, catalog_variant.id %>
  <%= submit_tag t("common.add_to_cart"), class: "btn-primary btn-cart" %>
<% end %>
