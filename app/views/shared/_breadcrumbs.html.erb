<%
  items = @breadcrumb_items || begin
                                 segments = request.path.split('/').reject(&:blank?)
                                 current_path = ""
                                 name_map = {
                                   "sortiment" => t("breadcrumbs.sortiment"),
                                   "palivove-drevo" => t("breadcrumbs.palivove_drevo"),
                                   "stavebni-rezivo" => t("breadcrumbs.stavebni_rezivo"),
                                   "truhlarske-rezivo" => t("breadcrumbs.truhlarske_rezivo"),
                                   "okrasne-kamenivo" => t("breadcrumbs.okrasne_kamenivo"),
                                   "vyrobni-zbytky" => t("breadcrumbs.vyrobni_zbytky"),
                                   "sluzby" => t("breadcrumbs.sluzby"),
                                   "kontakt" => t("breadcrumbs.kontakt"),
                                   "eshop" => t("breadcrumbs.eshop"),
                                   "kosik" => t("cart.page_title"),
                                   "obchodni-podminky" => t("legal.terms_title")
                                 }

                                 segments.map do |segment|
                                   current_path += "/#{segment}"
                                   [name_map[segment] || segment.tr('-', ' ').capitalize, current_path]
                                 end
                               end
%>

<% if items.any? %>
  <nav aria-label="<%= t('breadcrumbs.home_aria') %>" class="breadcrumb-nav">
    <%= link_to root_path, class: "breadcrumb-home" do %>
      <svg viewBox="0 0 24 24" width="24" height="24" fill="#5890FF">
        <path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"/>
      </svg>
    <% end %>

    <% items.each_with_index do |(name, path), index| %>
      <svg class="breadcrumb-separator" viewBox="0 0 24 24" width="20" height="20" fill="#000000">
        <path d="M8.59 16.59L13.17 12 8.59 7.41 10 6l6 6-6 6-1.41-1.41z"/>
      </svg>

      <%= link_to name, path, class: "breadcrumb-item #{'active' if index == items.size - 1}" %>
    <% end %>
  </nav>
<% end %>