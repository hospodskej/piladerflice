class CartsController < ApplicationController
  # A deliberately minimal cart page for now - just enough that "K
  # pokladně" in the added-to-cart modal has somewhere real to go. Editing
  # quantities, removing items, and checkout itself are the next phase.
  def show
  end
end
